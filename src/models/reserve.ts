import myDataSource from "../configs/common"

const createReservation = async (
  reservationName: string, phoneNumber: string, email: string,
  patientName: string, date: Date, time: string, hospital: string, clinicType: string  ) => {

}

export default { createReservation }